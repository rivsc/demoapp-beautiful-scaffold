
<%= form_for(@family) do |f| %>
  <% if @family.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@family.errors.count, "error") %> prohibited this family from being saved:</h2>

      <ul>
        <% @family.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %>
      </ul>
    </div>
  <% end %>
      <div class="form-group">
      <%= f.label :name, t('app.models.family.bs_attributes.name', :default => 'name').capitalize, :class => "control-label" %>
      <%= f.text_field :name, :class => "form-control" %>
    </div>
    <div class="form-group">
      <%= f.label :description, t('app.models.family.bs_attributes.description', :default => 'description').capitalize, :class => "control-label" %>
      <%= f.text_area :description, :class => "wysiwyg-editor form-control" %>
    </div>
    <%= f.hidden_field :description_typetext, :value => "html"  %>
<!-- Beautiful_scaffold - AddField - Do not remove -->
  <div>
    <%= link_to families_path, :class => "btn btn-light" do %>
      <i class="fa fa-chevron-left"></i> <%= t(:back, :default => "Back") %>
    <% end %>
    <%= f.submit :class => "btn btn-primary", :data => { :disable_with => t(:saving, :default => "Saving...") } %>
  </div>
<% end %>


