<h2><%= t(:show, :default => "Show") %> <%= t('app.models.family.bs_caption', :default => 'family') %></h2>

  <p>
    <b><%= t('app.models.family.bs_attributes.name', :default => 'name') %>:</b>
      <%= @family.name %>
  </p>
  <p>
    <b><%= t('app.models.family.bs_attributes.description', :default => 'description') %>:</b>
      <%= @family.description %>
  </p>

<!-- Beautiful_scaffold - AddField - Field - Do not remove -->

<%= link_to t(:edit, :default => "Edit"), edit_family_path(@family), :class => "btn btn-light" %>
<%= link_to t(:back, :default => "Back"), families_path, :class => "btn btn-light" %>

